require "spec_helper"

RSpec.describe Now::Showing do
  it "has a version number" do
    expect(Now::Showing::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
